/**
 * Top-down tileset constants.
 * Ground assets from Craftpix village/fields pixel tilesets (AGENTS.md).
 */

/** Top-down tile size – square cells (32×32, matches asset sheets). */
export const TILE_SIZE = 32;

/** Number of tiles in the main ground tileset (8×8 sheet). */
export const GROUND_TILESET_TILE_COUNT = 64;
